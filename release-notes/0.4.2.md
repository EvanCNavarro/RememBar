- Refreshed the About screen into a cleaner, more consistent card — app icon, version, a one-line description of what RememBar does, and quick GitHub + License links
- Polished the search bar: the clear (×) and return buttons now match the field's height and corner rounding and share the same hover highlight, so the whole row lines up instead of looking mismatched
- Fixed the Settings window opening far too tall, with a big empty gap below the About tab — it now sizes to its content
- Under the hood: RememBar now shares one design system with its companion app (so the two look and feel consistent), and the test suite is steadier on a busy machine
