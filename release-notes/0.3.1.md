- Fixes a crash on launch introduced in 0.3.0: the app couldn't find its bundled icon and quit immediately on some machines
- Bundled resources now load reliably from the app itself, so a missing resource degrades gracefully instead of crashing
- Everything from 0.3.0 (the redesigned update experience and in-window "What's new") now actually reachable
