- Fixed 1Password guidance: when 1Password needs attention, RememBar no longer sends you to Full Disk Access (which never applied to it) — it now points you to enabling the 1Password CLI, the setting that actually matters
- A 1Password that isn't connected yet now shows as a calm, optional hint rather than an alarming permission warning — it's an integration you turn on, not access you lost
- Note: durable 1Password results across updates still depend on signed releases (coming later); for now, enabling "Integrate with 1Password CLI" in 1Password's Developer settings is what lets RememBar read your item titles
