- Live search: results now appear as you type — no need to press Enter. Enter still searches immediately, and a couple of characters are enough to start
- Results stay on screen while the next search runs, so the list no longer blanks and flickers between keystrokes; an empty search now shows a clear "No matches" instead of a blank panel
- Term Families editor: manage your search synonyms in-app (the "?" panel → "…" → Term Families…) instead of hand-editing a JSON file — add families and words as chips, and changes apply to your very next search with no restart
- Under-the-hood: the whole search flow was consolidated onto one dispatch path (steadier, less redundant work) and the term-family engine is fully covered by tests
