- Redesigned software update experience: checking, downloading, and installing now use RememBar's own compact interface instead of the generic system dialog
- "What's new" appears right in the update window, as a tidy expandable list of the changes in each release
- The update window morphs smoothly between steps rather than jumping between separate dialogs
- Clearer wording throughout the flow — "Install Update", then "Restart Now"
- About panel polish: the real app icon, matched controls, and hover feedback on every button
